node_modules/
dist/
.vite/
.env
.env.local
*.local
.DS_Store
*.log

# Config reale (la TUA infra) — nel repo va solo services.example.yaml
services.yaml

# Planning: fuori dal repo OSS (vivono qui solo per la revisione locale)
REQUIREMENTS.md
HANDOFF.md

# Terraform (ricetta deploy/terraform): stato + tfvars reali fuori dal repo
*.tfvars
!*.tfvars.example
.terraform/
*.tfstate
*.tfstate.*
