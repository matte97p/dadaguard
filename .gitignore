node_modules/
dist/
.vite/
.env
.env.local
*.local
.DS_Store
*.log

# Config reale (la TUA infra) — nel repo va solo services.example.yaml
services.yaml

# Render di demowright che NON sono asset del README (le versioni italiane, il verticale per i
# social): si rigenerano con un comando dallo script, e un mp4 da 10 MB per lingua in cronologia
# git è peso che non torna più indietro. Nel repo sta solo `assets/demo.mp4` + la GIF.
output/

# Planning: fuori dal repo OSS (vivono qui solo per la revisione locale)
REQUIREMENTS.md
HANDOFF.md

# Terraform (ricetta deploy/terraform): stato + tfvars reali fuori dal repo
*.tfvars
!*.tfvars.example
.terraform/
*.tfstate
*.tfstate.*

# I worktree git di questo repo: cartelle di lavoro, non contenuto. Committarli aggiunge un
# "embedded git repository" che in un clone arriva vuoto (visto il 31/08/2026, `git add -A`).
.worktrees/
